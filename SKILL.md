---
name: master-cv-jd-tailor
description: 以用户确认过的 Master CV 为唯一事实库，根据目标 JD 定制中文一页简历。适用于“根据 JD 改简历”“针对岗位定制简历”“从 Master CV 里筛选岗位版本”等请求。流程包括：JD 拆解、匹配与缺口判断、必要追问、事实约束下的经历选择与改写、保持原有 section 与视觉体系生成 Word、PDF 一页与版面利用率验证。默认不编造经历、不机械套 JD 关键词、不随岗位自动换模板。
---

# Master CV → JD 定制简历 Skill

## 目标

以经过用户确认的 Master CV 作为 Single Source of Truth，根据具体 JD 对已有经历进行选择、排序、压缩和业务化改写，生成一份：

- 高度匹配目标岗位；
- 不编造事实；
- 保持用户固定简历结构与视觉体系；
- 中文自然、专业、不过度英文化；
- 一页 A4，且页面利用率合理；
- 可直接用于投递的 `.docx` 简历。

核心思想：

> JD 决定“选什么、强调什么、怎么写”，但不能反过来创造用户没有的经历。

---

## 触发条件

当用户提供目标 JD，并提出以下意图时使用：

- 根据 JD 改简历
- 针对某岗位定制简历
- 从 Master CV 中提炼岗位版本
- 分析岗位匹配度并生成投递版 Word
- 对已有一页简历做 JD 定向改写

若用户尚未提供 Master CV，可使用其已上传且确认过的完整简历作为事实库；若多个版本存在冲突，优先使用用户最新确认的版本，并询问关键冲突。

---

# 工作流

## 第 1 步：读取事实库，而不是先写简历

先读取：

1. 用户当前确认的 Master CV；
2. 用户在对话中明确补充过的真实经历信息；
3. 目标 JD。

Master CV 是唯一事实源。允许补充用户在当前对话明确确认的新事实，但不要用模型推测补全经历。

### 事实强度分级

- A 级：用户明确确认 + 简历/文件有支持 → 可直接写
- B 级：用户明确确认，但文件没有 → 可写，措辞保持克制
- C 级：从材料可合理推断，但用户未确认 → 不作为强结论；必要时追问
- D 级：仅为了贴 JD 而猜测 → 禁止写

如果某个事实会明显影响 bullet 强度，优先问用户，例如：

- 你是参与、负责还是主导？
- 最后有没有上线/交付/形成报告？
- 这个结果是团队结论还是你个人结论？
- 你是否真的操作过某广告平台/数据工具？
- 业务结论或最终动作是否确定？

不要为了“自动化”跳过必要追问。

---

## 第 2 步：拆解 JD

在修改简历前，先把 JD 拆成：

1. 3–5 个核心职责
2. 硬性要求
3. 加分项
4. 岗位真正看重的能力
5. 用户的直接匹配点
6. 用户的明显缺口

### 缺口处理

缺口必须明确告诉用户，但不要因为存在缺口就直接判断“不能投”。

例如：

- JD 要求 Meta / Google Ads 实操，用户没有 → 明确写为 gap；不能虚构投放经验
- 用户有内容增长与渠道分析 → 可以说明这些是相邻能力，但不能写成“熟练海外广告投放”

---

## 第 3 步：做经历匹配与篇幅分配

对 Master CV 中每个真实经历 / workstream 按 JD relevance 分为：

- 高相关：重点展开，通常保留 1–2 条完整 bullet
- 中相关：保留 1 条，突出最相关能力
- 低相关：压缩为极短内容，或在空间不足时删除

### 关键原则

JD 定制的重点不是机械替换关键词，而是“篇幅分配”。

例如：

- 最相关项目写得更完整
- 相关实习保留最匹配的 bullet
- 与岗位弱相关的经历压缩
- Skill 栏同步重排

---

## 第 4 步：默认保留 Master CV 的 section 结构

不要为了“最相关经历排第一”而随意把不同性质经历合并成“相关经历”。

默认结构：

1. 教育背景
2. 实习经历
3. 项目与运营经历
4. 其他经历
5. 技能与兴趣

可根据用户原简历实际 section 名称微调，但默认：

- 实习仍属于“实习经历”
- 项目仍属于“项目与运营经历”
- TA / 社团仍属于“其他经历”

### 什么时候允许调整 section

只有在以下情况才调整：

- 用户明确要求；
- 用户原简历本身就采用其他结构；
- 某类经历完全不存在；
- 为英文简历等特殊格式做必要变更。

不要仅因为某个项目最匹配 JD，就跨 section 搬家。

---

## 第 5 步：一个真实 workstream = 一个小标题 + 一个完整 bullet

不要把同一个项目的步骤人为拆成多个能力点来“凑 bullet”。

错误示例：

- 市场调研：……
- 竞品分析：……
- 财务测算：……
- 投资建议：……

如果它们其实都属于一个“长租公寓收购咨询”项目，应写成：

- 收购咨询：业务背景 → 实地/市场调研 → 竞品分析 → 财务测算 → 已知结论/交付

### 推荐 bullet 结构

`业务能力/项目类型：业务场景 + 你的动作 + 方法/数据 + 产出/已知结果`

尽量形成完整的 Problem → Method → Deliverable → Stakeholder 链路。

只有已确认的 Business Impact 才能写。

---

## 第 6 步：小标题优先写“业务能力”，不是工具名

优先：

- 渠道增长
- 产品策略
- 市场洞察
- 收购咨询
- 内容策略
- 商业化运营
- 数据治理

避免：

- SQL 分析
- Python 分析
- Power BI
- 数据清洗

工具和方法放正文里。

### 与 JD 关键词的关系

可以让小标题靠近 JD 能力语言，但不要机械复制 JD 原词。

优先级：

1. 真实
2. 自然
3. 专业
4. 与 JD 接近

---

## 第 7 步：中文优先，不为了“专业感”堆英文

默认使用自然中文。

不必要的英文术语应改成中文，例如：

- Voice of Customer → 客诉与用户反馈 / 客诉与售后表现
- Customer Segmentation → 用户分层
- Lifecycle Funnel → 生命周期漏斗
- Comparable Analysis → 可比项目分析
- Post-purchase Customer Journey → 购后用户旅程

行业标准名词、工具名、指标名可保留：

- SQL
- Python
- Power BI
- LEED
- ESG
- NOI
- Cap Rate
- IRR

判断标准：目标行业真实工作中是否通常这么说，而不是“英文是否显得高级”。

---

## 第 8 步：量化结果有则优先，没有不要硬凑

真实、有意义的数字优先保留，例如：

- 独立完成 20+ 个项目推介材料
- 数万粉增长至百万粉级
- 多条内容获得 10 万至 100 万+ 点赞
- 3 分钟归因窗口

禁止：

- 编造“效率提升 30%”
- 编造转化率提升
- 用无业务意义的大表行数凑量化

### 区分相关性与因果性

可以写：

“转型后账号由数万粉增长至百万粉级”

不要写：

“通过我的策略使账号增长 100 万粉”

除非因果确实可证明。

---

## 第 9 步：处理结果与影响时遵守证据层级

结果表述优先级：

1. 已确认业务结果：上线、交付、正式采用、明确投资结论
2. 已确认分析产出：订单级结果、漏斗、BI 看板、报告、数据底表
3. 下游决策未知：只写“支持评估 / 提供分析依据”，不要写“推动改版 / 提升转化”

对中台数据岗位尤其注意：

产品提出需求 → 数据侧定义口径/分析/建报表 → 交付产品 → 后续动作未知

若用户不知道产品后续动作，就不要替用户补业务影响。

---

## 第 10 步：Skills 必须随 JD 变化

不要机械复制 Master CV 的全部 Skills。

选择原则：

- JD 明确要求且用户真实具备 → 优先
- 相关工具/方法 → 保留
- 与目标岗位关系弱 → 删除或降权

例如市场/游戏岗位可突出：

- 内容运营
- 渠道 / 转化分析
- 市场研究
- 游戏经历
- Excel / SQL / Python
- 英文能力

而不必保留与岗位无关的 NOI / Cap Rate / IRR。

---

## 第 11 步：先给用户“定制策略”，必要时再追问

不要一看到 JD 就立刻生成文件。

先简要说明：

- JD 最看重什么
- 用户最强匹配点
- 明显 gap
- 准备保留/强化哪些经历
- 准备压缩/删除哪些内容

若存在会显著影响简历的未知事实，先问用户，再生成。

---

# 排版与视觉规则

## 1. 默认继承用户 Master CV 的视觉体系

不要根据岗位自动换配色或模板。

默认：

- 黑白 / 深灰简洁风
- A4 一页
- section 标题 + 横向分隔线
- 机构 / 项目名清晰
- 日期右对齐
- bullet 层级稳定
- 中文可读性优先

“改 JD”默认只改内容，不随意重做整体模板。

只有用户主动要求“咨询风 / 投行风 / 互联网风 / 彩色版”等时再调整视觉。

## 2. 默认不自动加照片

- 用户原简历无照片 → 不加
- 用户原简历有照片 → 保留
- 用户明确要求 → 添加/删除

## 3. Section 横线属于模板的一部分

生成或改写时必须保留。不要只复制标题字体而遗漏段落底边框。

## 4. 教育背景标点遵循用户模板

若用户模板教育背景行末无句号，则保持无句号；不要自动统一为带句号。

---

# 一页验证标准

“一页”不等于仅 `page_count == 1`。

必须同时满足：

- PDF 页数 = 1
- 页面整体利用率合理，目标约 85%–95%
- 不出现下半页大片空白
- 不为了填满而加入低价值废话
- 无孤行、断裂、重叠、溢出
- section 横线存在
- 机构名、职位、日期对齐正常
- 字号足够可读
- 电话、邮箱零错误

### 调整顺序

当内容太少：

1. 优先恢复更有价值的真实经历 / bullet
2. 展开高相关经历的完整分析链路
3. 补充真实的 Skills / 游戏 / 海外背景等 JD 加分信息
4. 微调 section spacing
5. 最后才适当放大字号或边距

当内容超过一页：

1. 删除低相关内容
2. 压缩重复 bullet
3. 缩短修饰词，保留动作 + 方法 + 结果
4. 压缩 section spacing
5. 最后才小幅缩字号和页边距

不要优先通过把正文缩到很小来“硬塞一页”。

---

# Word 生成建议

优先基于用户当前 Master CV 模板直接修改，以保持版式一致。

如果必须从代码生成，可参考 `scripts/resume_template.py`，但必须满足：

- section 名称与用户原简历一致
- section 标题底部横线保留
- 默认黑白/深灰，不自动蓝色化
- 照片默认关闭
- 中文字体正确设置 `w:eastAsia`
- 日期使用右对齐 tab 或稳定表格布局

---

# 验证流程（必做）

运行：

```bash
python3 scripts/verify_resume.py <resume.docx> [preview_dir]
```

该脚本应至少完成：

1. DOCX → PDF
2. 检查 PDF 是否只有 1 页
3. 输出页面内容底部位置 / 留白占比等辅助指标
4. 渲染预览图

然后必须人工查看预览，确认：

- 一页
- 基本填满
- 横线正常
- 联系方式正确
- section 完整
- 无排版错误

自动指标只能作为辅助，不能代替视觉检查。

---

# 交付规则

默认交付：

- `.docx` 投递版
- 可选 `.pdf` 仅用于校验或用户需要时交付

文件名：

`姓名_公司_岗位_2027校招.docx`

交付说明保持简短：

- 这版重点强化了什么
- 哪些内容对应 JD
- 仍存在什么真实 gap

不要在交付时重复整篇简历。

---

# 禁止事项

- 不编造经历、技能、平台实操经验
- 不伪造业务结果或提升比例
- 不把团队结果全部归因给个人
- 不把“分析支持”写成“推动上线”，除非有证据
- 不把一个真实项目拆成多个伪 workstream
- 不机械复制 JD 用词作为每个 bullet 标签
- 不为了显得专业堆英文
- 不因岗位变化自动换模板、颜色、照片
- 不以“PDF 只有一页”作为唯一版式验收标准
